import { AppBar, Toolbar } from "@material-ui/core";

const Header = () => {
  return (
    <AppBar position="static">
      <Toolbar>It is a basic list!</Toolbar>
    </AppBar>
  );
};

export default Header;
